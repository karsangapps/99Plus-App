// Focus mode: bare layout, no global nav — all chrome lives inside DrillEngineShell
export default function DrillSessionLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
